@using AkGida_MyInfo.Models
@model IEnumerable<Death>

@foreach (var item in Model)
{
    var personel = item.Name + " " + item.Surname;
    <li><a href="portfolio-grid-2-columns.html">@personel</a></li>
}